{
  "version": 2,
  "builds": [
    {
      "src": "myproj/wsgi.py",
      "use": "@vercel/python",
      "config": { "maxLambdaSize": "500mb", "runtime": "python3.12" }
    }
  ],
  "routes": [
    {
      "src": "/static/(.*)",
      "dest": "/static/$1"
    },
    {
      "src": "/(.*)",
      "dest": "myproj/wsgi.py"
    }
  ],
  "installCommand": "pip install -r requirements.txt --no-cache-dir",
  "buildCommand": "bash build.sh"
}